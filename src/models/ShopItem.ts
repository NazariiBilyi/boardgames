import mongoose from "mongoose";

const Schema = mongoose.Schema;

const ShopItem = new Schema({
    name: {
        type: String,
        required: true
    },
    type: {
        type: String,
        required: true
    },
    price: {
        type: Number,
        required: true
    }

})